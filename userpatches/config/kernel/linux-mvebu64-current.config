# MACCHIATObin Double Shot — CP110 SERDES PHY fix
# Without this, all CP110-attached devices (Ethernet, SATA, PCIe, USB) fail
# with "deferred probe pending" at boot and never come up.
CONFIG_PHY_MVEBU_CP110_COMPHY=m
